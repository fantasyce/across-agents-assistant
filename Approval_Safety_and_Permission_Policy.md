# Across Agents Assistant - 审批、安全与权限策略

## 1. 文档目标
本文档用于定义 Across Agents Assistant 在 MVP 阶段的安全边界，包括审批策略、风险分级、工具白名单、权限申请原则、敏感数据处理要求和审计约束。

## 2. 安全设计原则
- 默认保守，能不执行就先不执行。
- 默认草稿优先，不直接外发。
- 默认最小权限，不提前申请无关权限。
- 默认最小采集，不默认发送截图和高敏上下文。
- 所有高风险动作都必须可解释、可审批、可追踪。

## 3. 风险分级模型
### 3.1 L0：纯问答
定义：
- 仅进行文本理解、总结、翻译、改写和建议。

示例：
- 总结当前页面。
- 解释选中的代码。

策略：
- 不审批。
- 记录任务日志即可。

### 3.2 L1：低风险只读
定义：
- 读取本地或系统上下文，但不产生外部副作用。

示例：
- 读取剪贴板。
- 获取前台应用和窗口标题。
- 读取工作区内文本文件。
- 获取 git 状态。

策略：
- 默认不审批。
- 必须在 UI 中展示本轮读取了哪些来源。
- 敏感来源需要高亮说明。

### 3.3 L2：受控可逆写入
定义：
- 产生写入动作，但目标可控、结果通常可撤销或可人工复核。

示例：
- 创建邮件草稿。
- 生成笔记草稿。
- 在工作区指定目录写入文件。

策略：
- 默认需要审批。
- 审批弹窗必须展示写入目标和写入摘要。

### 3.4 L3：高风险不可逆或外发
定义：
- 可能导致不可逆影响、对外发送或系统环境改变。

示例：
- 删除文件。
- 发送邮件或消息。
- 执行终端命令。
- 修改系统设置。

策略：
- 必须强审批。
- MVP 默认不开放或仅在明确受控场景中开放。

## 4. 审批决策规则
审批不仅看工具名称，还必须综合以下因素：
- 动作类型
- 目标对象
- 是否外发
- 是否可逆
- 读写范围
- 用户当前模式
- 当前应用环境

### 4.1 审批触发条件
以下情况必须触发审批：
- 任意写操作。
- 任意外发行为。
- 任意终端命令执行。
- 任意系统设置修改。
- 任意截图/OCR 且用户未开启长期授权策略。
- 任意跨目录或越出工作区的文件写入。

### 4.2 可免审批条件
以下情况通常可免审批：
- 纯文本回答。
- 读取前台应用和窗口标题。
- 读取剪贴板。
- 读取工作区内白名单文本文件。

前提：
- 数据读取来源要在 UI 中可见。

## 5. 审批 UI 要求
审批弹窗必须显示：
- 用户原始目标
- Agent 计划摘要
- 将读取的数据来源
- 将写入或发送的目标
- 风险等级
- 将调用的工具列表
- 用户可选动作

### 5.1 用户可选动作
- `approve_once`
- `reject`
- `convert_to_draft`
- `cancel`

### 5.2 MVP 不建议提供的能力
- “始终允许所有高风险动作”
- “永不再提醒”
- 对过大范围的长期授权

## 6. 权限策略
### 6.1 麦克风权限
用途：
- 语音输入和录音。

策略：
- 在首次使用语音输入时请求。
- 被拒绝后降级为文本输入模式。

### 6.2 Accessibility 权限
用途：
- 获取窗口标题、前台元素信息、部分应用的选中文本和自动化能力。

策略：
- 在首次使用相关能力时请求。
- 若未授权，仍可保留纯文本和剪贴板场景。

### 6.3 屏幕录制权限
用途：
- 当前窗口或屏幕截图。

策略：
- 只有用户开启截图/OCR 时再请求。
- 默认关闭，不应在首次启动时申请。

## 7. 工具白名单策略
### 7.1 允许的工具类型
- 读取类工具
- 草稿类工具
- 少量受控外部动作工具

### 7.2 白名单要求
每个工具必须具备：
- 明确功能说明
- 输入输出 schema
- 风险等级
- 审批要求
- 路径或资源访问范围
- 超时限制
- 错误码定义

### 7.3 禁止项
- 任意 Shell 命令执行
- 任意 Python/Node 脚本执行
- 任意文件系统写入
- 无边界的网络请求代理
- 无审批的外发动作

## 8. Safe Command 策略
如果 MVP 后续引入 `safe_command`，必须满足：
- 命令在白名单中。
- 参数模式受限。
- 不支持 shell 拼接。
- 不支持管道、重定向、子命令、环境变量注入。
- 限制工作目录。
- 限制超时和输出大小。
- 视为高风险能力。

## 9. 文件系统策略
### 9.1 读取限制
- 默认只允许读取工作区或用户显式选择的路径。
- 默认只读文本类文件。
- 对超大文件要限制读取大小。

### 9.2 写入限制
- 默认仅允许写入工作区内白名单目录。
- 写入前必须生成变更预览摘要。
- 非草稿性质的写入默认视为高风险。

### 9.3 删除限制
- MVP 默认不支持直接删除。
- 如未来支持，也必须强审批并优先移动到废纸篓。

## 10. 敏感数据处理
### 10.1 高敏数据类型
- 剪贴板中的账号、密码、验证码、支付信息
- 截图中的隐私信息
- 音频原始数据
- 邮件正文、聊天内容和个人识别信息

### 10.2 处理要求
- 默认短时缓存。
- 任务历史中优先存摘要，不长期保存原文。
- 敏感数据不应在无提示情况下上传。
- 用户应可以清空历史和缓存。

## 11. 日志与审计策略
### 11.1 必须记录
- 请求 ID
- 任务开始和结束时间
- 使用了哪些上下文来源
- 风险等级
- 是否触发审批
- 审批决策
- 调用了哪些工具
- 是否执行成功
- 错误码

### 11.2 不建议明文记录
- 长段剪贴板内容
- 完整截图原图
- 原始音频
- 邮件或消息的全部正文

## 12. 失败与降级策略
### 12.1 权限缺失
- 提示权限用途。
- 提供系统设置引导。
- 自动降级为可用能力。

### 12.2 审批拒绝
- 停止执行。
- 返回替代方案，例如只输出草稿文本。

### 12.3 工具失败
- 返回结构化错误。
- 说明失败原因和可重试建议。

### 12.4 模型失败
- 返回兜底提示。
- 保留用户输入和上下文摘要供重试。

## 13. 明确的安全红线
- 未授权情况下不得截屏。
- 未审批情况下不得执行写入、外发或终端命令。
- 未声明范围的工具不得访问本地资源。
- 不得把“模型建议”当作“用户授权”。

## 14. 安全审查清单
每次新增工具前，至少回答以下问题：
- 是否真的需要这个工具？
- 是否存在更低风险替代方式？
- 工具是否有清晰边界？
- 工具是否可预览和审计？
- 工具失败时是否有安全降级？
- 工具是否会接触高敏数据？

## 15. 文档联动要求
- 新增工具时，必须同步更新 `Context_Pack_and_Tool_Protocol.md`。
- 变更风险分级时，必须同步更新 `MVP_PRD.md`。
- 变更审批流程时，必须同步更新 `Interaction_Flows_and_State_Machine.md`。
- 变更权限申请方式时，必须同步更新测试与发布计划。
