# Across Agents Assistant - 项目进度与交接文档

**日期**: 2026-04-22
**当前分支**: `feature/mac-native-mvp`
**当前阶段**: Phase 4 (里程碑 M4 已达成核心闭环)

---

## 🌟 一、当前项目状态与已达成里程碑

我们已经成功将项目推进至 **Phase 4 (场景价值验证)**。核心的混合架构（Swift + Python）、系统上下文抓取、大模型意图规划以及安全审批链路均已彻底跑通。

### 1. 基础架构与交互入口 (Phase 0 - 1)
- **混合架构落地**：确立了 `macOS-Client/` (SwiftUI 原生客户端) 和 `backend/` (Python 业务与大模型调度) 的职责边界。
- **原生 UI 体验**：完成了 Menu Bar 常驻、毛玻璃悬浮窗以及对话气泡的开发，支持文本高亮与复制。
- **全局快捷键**：实现了基于底层 API 的 `Cmd+Option+Space` 全局唤醒，丝滑且不与 IDE 冲突。

### 2. 上下文与多模态交互 (Phase 2)
- **Tier 1 上下文采集**：攻克了 macOS 焦点抢占难题。通过在唤起前快照记录 PID 并监听 `NSWorkspace` 的失焦通知，实现了对真实“上一个活跃应用”（如 Finder、Xcode）的精准识别。
- **原生语音播报 (TTS)**：废弃了臃肿的 Python TTS 方案，深度接入 macOS 原生的 `AVSpeechSynthesizer`，实现了低延迟、高自然度的系统语音播报。

### 3. 受控执行与安全审批 (Phase 3)
- **工具注册中心 (Tool Registry)**：在 Python 后端建立了标准化的工具注册机制，支持动态注入 Tool Schema 给大模型。
- **红绿灯审批流 (Approval Flow)**：这是系统的**安全底线**。大模型生成的工具调用意图会被解析并拦截，通过 SwiftUI 弹出原生的“高/中/低”风险审批弹窗。支持混合类型参数（通过自定义 `AnyCodableValue` 解决 Swift 强类型解析报错）。

### 4. 真实场景适配与 LLM 深度整合 (Phase 4)
- **大脑接入**：剥离了早期的 Mock 逻辑，成功将真实的 LLM (OpenClaw) 接入链路，实现了从“自然语言 -> 意图推理 -> JSON Tool Call”的真实闭环。
- **高级场景适配器 (Tier 2)**：
  - **浏览器**：实现了 `get_active_browser_url`，支持动态选择 Chrome/Safari，且能穿透后台静默读取最后活跃的标签页。
  - **Finder (访达)**：实现了 `get_finder_context`，精准获取当前目录和高亮选中的文件绝对路径。
  - **Xcode**：实现了 `get_xcode_context`，获取当前正在编辑的代码文件路径。
  - **系统控制**：实现了深色模式切换和系统音量控制（并优雅处理了外接 HDMI/DP 显示器导致的硬件锁定报错 `missing value`）。

---

## 🎯 二、后续开发计划 (Phase 5 & 商业化准备)

M4 的核心链路已经打通，接下来的开发重心将从“堆功能”转向**“体验打磨、稳定性和发布准备”**，即冲击 **M5 (Beta 版发布)**。

### 1. 权限引导与错误恢复 (高优)
- **优雅的权限申请**：目前读取浏览器、操作 Finder 强依赖 macOS 的“辅助功能”和“自动化”权限。需要开发一个美观的权限引导弹窗，手把手教小白用户去系统设置里开启权限，而不是直接让脚本静默报错。
- **容错与降级**：当跨应用提取文本失败时（例如某些不支持 AppleScript 的小众应用），向 LLM 和用户提供优雅的错误提示或降级方案（如：“请先手动复制到剪贴板”）。

### 2. 记忆与可观测性 (中优)
- **本地 SQLite 数据库**：目前会话状态是内存级的。需要接入轻量级本地数据库，持久化用户的聊天记录和审批日志（审计追踪）。
- **Debug 模式**：完善后端的 Log 记录体系，方便在内测阶段快速定位 LLM 幻觉或脚本执行失败的原因。

### 3. 视觉上下文 (Tier 3) 与生态扩展 (长线)
- **屏幕截图与 OCR**：对于没有专属适配器的应用，引入按需触发的截图和文字识别能力，真正做到“所见即所问”。
- **更多工具接入**：基于用户的真实内测反馈，扩展更多高价值的系统级工具（如操作日历、邮件读取、VSCode 专属插件联动等）。

---

## 🛠️ 三、交接与环境配置提示

1. **前后端分离启动**：
   - 前端：在 `macOS-Client` 目录下使用 Xcode 打开或使用 `swift build` 编译运行。
   - 后端：在 `backend` 目录下使用 `PYTHONPATH=src python3 main.py` 启动 API 服务（默认端口 8000）。
2. **调试技巧**：
   - 如果遇到大模型拒绝调用工具，请查看后端控制台打印的 `[DEBUG] 发送给大模型的 PROMPT`，检查 `【系统上下文】` 是否正确抓取到了目标应用。
   - 所有的 macOS 原生能力扩展都在 `backend/src/across_agents_assistant/tools/builtin_tools.py` 中，采用 AppleScript 实现。新增工具时请务必做好异常捕获（`try-except`）。

祝你在新的开发环境中一切顺利！MVP 的核心地基已经非常牢固，期待你为它添砖加瓦！🚀