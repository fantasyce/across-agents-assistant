# Across Agents Assistant - 测试与发布计划

## 1. 文档目标
本文档用于定义 Across Agents Assistant MVP 阶段的测试范围、验收标准、测试矩阵、发布流程、灰度策略和发布阻断条件，确保产品在安全、稳定和体验上具备最低发布质量。

## 2. 测试目标
- 验证主链路可用性。
- 验证审批与权限边界可靠性。
- 验证高风险动作不会在未授权条件下执行。
- 验证在失败、超时和权限拒绝场景下具备可用的降级体验。

## 3. 测试范围
### 3.1 P0 必测
- 菜单栏启动与主窗口打开。
- 全局快捷键。
- 文本输入。
- 语音输入与 STT。
- Tier 1 上下文采集。
- 普通问答。
- 工具白名单执行。
- 审批弹窗。
- 任务日志。
- 权限缺失处理。

### 3.2 P1 建议测试
- 浏览器适配器。
- Finder 适配器。
- IDE 适配器。
- 草稿工具兼容性。
- 历史记录与恢复。

### 3.3 P2 可延后测试
- 截图与 OCR。
- `safe_command`。
- 更复杂的多步任务。

## 4. 测试维度
### 4.1 功能测试
- 输入、上下文、规划、审批、执行、反馈全流程。

### 4.2 权限测试
- 麦克风、Accessibility、屏幕录制在授权、拒绝、撤销后的行为。

### 4.3 安全测试
- 未审批时写操作、外发操作和命令执行是否被正确阻止。
- 工具是否越权访问路径和资源。

### 4.4 稳定性测试
- 长时间驻留。
- 多次唤起。
- 重复任务。
- 快速取消与重试。

### 4.5 性能测试
- 唤起延迟。
- 首个可见反馈时间。
- 普通问答耗时。
- 审批弹窗显示耗时。

## 5. 测试矩阵
| 编号 | 测试项 | 预期结果 | 优先级 |
|---|---|---|---|
| T001 | 启动后状态栏图标存在 | 应用稳定驻留，无崩溃 | P0 |
| T002 | 点击状态栏图标打开窗口 | 窗口正常显示并获得焦点 | P0 |
| T003 | 快捷键唤起 | 快捷键可稳定触发，无重复实例 | P0 |
| T004 | 文本提问 | 返回模型回答，流程完整 | P0 |
| T005 | 语音输入 | 录音、转写、结果显示完整 | P0 |
| T006 | 读取前台应用与窗口标题 | 上下文摘要正确显示 | P0 |
| T007 | 读取剪贴板 | 剪贴板内容可用于回答 | P0 |
| T008 | 只读工具调用 | 工具成功执行或返回结构化错误 | P0 |
| T009 | 草稿工具调用 | 审批后生成草稿，不直接发送 | P0 |
| T010 | 高风险动作审批 | 未同意前不得执行 | P0 |
| T011 | 审批拒绝 | 任务停止并返回替代建议 | P0 |
| T012 | 麦克风权限拒绝 | 提示权限并降级到文本输入 | P0 |
| T013 | Accessibility 权限拒绝 | 提示权限并降级能力 | P0 |
| T014 | 屏幕录制权限拒绝 | 不截屏并提供替代方案 | P1 |
| T015 | 模型超时 | 返回可解释错误，可重试 | P0 |
| T016 | 工具超时 | 返回 timeout，记录日志 | P0 |
| T017 | 取消任务 | 正确停止任务并清理状态 | P0 |
| T018 | 浏览器适配器 | 可获取 URL 或页面标题 | P1 |
| T019 | Finder 适配器 | 可获取当前目录或选中文件 | P1 |
| T020 | 截图与 OCR | 按需采集、权限受控、时延可接受 | P2 |

## 6. 关键验收场景
### 场景 A：当前页面总结
- 前置条件：
  - 应用已启动。
  - 当前处于浏览器窗口。
- 操作：
  - 用户通过快捷键唤起并输入“总结当前页面”。
- 预期：
  - 系统采集前台应用、窗口标题和必要上下文。
  - 返回可读摘要。
  - 不触发审批。

### 场景 B：创建邮件草稿
- 前置条件：
  - 剪贴板中有背景信息。
- 操作：
  - 用户输入“帮我基于当前内容起草一封客户跟进邮件”。
- 预期：
  - 系统展示审批弹窗。
  - 用户同意后创建邮件草稿。
  - 不直接发送。

### 场景 C：权限缺失降级
- 前置条件：
  - 未授予 Accessibility 权限。
- 操作：
  - 用户请求读取当前窗口更深层上下文。
- 预期：
  - 系统提示权限不足。
  - 给出授权路径或剪贴板替代方案。

## 7. 自动化与手工测试建议
### 7.1 自动化优先覆盖
- 协议对象序列化和反序列化。
- 风险分级规则。
- 工具 schema 校验。
- Planner 输出解析。
- 状态机转换逻辑。

### 7.2 手工测试优先覆盖
- 权限申请和系统交互。
- 状态栏与窗口行为。
- 审批弹窗体验。
- 语音输入完整体验。
- 跨应用适配器行为。

## 8. 非功能验收标准
### 8.1 性能
- 快捷键到窗口可见时间小于 100ms。
- 普通问答首个反馈时间小于 500ms。
- 审批弹窗出现时间小于 500ms。

### 8.2 稳定性
- 连续 50 次唤起和关闭不崩溃。
- 连续 20 次问答不出现状态错乱。

### 8.3 安全
- 未审批时无任意写入、外发和命令执行。
- 权限拒绝时无越权读取。

## 9. 发布流程
### 9.1 Alpha 发布
- 面向内部开发和少量产品验证。
- 关注主链路是否打通。
- 接受一定数量的非核心缺陷。

### 9.2 Beta 发布
- 面向种子用户。
- 要求主链路稳定。
- 权限、审批、安全边界必须通过。

### 9.3 正式发布前检查
- 关键崩溃关闭。
- 安全红线关闭。
- 已知问题可控且有说明。
- 核心场景成功率达到目标。

## 10. 发布阻断条件
出现以下任一情况不得发布：
- 存在未审批写入或外发。
- 存在未授权截图或越权读取。
- 核心流程高概率崩溃。
- 审批 UI 信息不完整，用户无法理解风险。
- 权限拒绝后出现功能假成功或静默失败。

## 11. 灰度与反馈
### 11.1 内测范围
- 首批 5 到 10 名高频 macOS 用户。
- 优先覆盖开发者、产品经理和重度浏览器用户。

### 11.2 收集指标
- 日活和周活
- 任务完成率
- 审批通过率
- 权限授权率
- 失败类型分布
- 首次价值实现时间

### 11.3 反馈机制
- 每周整理失败案例。
- 分类为权限、上下文、模型、工具、UI 五类问题。
- 高优先级问题回灌到 `Engineering_Task_Breakdown.md`。

## 12. 发布清单
发布前至少完成：
- 安装和卸载说明
- 权限说明文档
- 已知限制说明
- API Key 配置说明
- 崩溃日志收集方式
- 回滚方案

## 13. 回滚策略
- 保留上一个稳定构建版本。
- 新版本出现严重审批或权限问题时立即停止扩散。
- 对内测用户提供回退安装包或旧版本恢复说明。

## 14. 文档联动要求
- 新增测试场景时，需检查 `MVP_PRD.md` 和交互状态机文档是否同步。
- 新增工具和权限后，需补充安全测试项。
- 路线图延期或范围变更时，需重新调整 P0/P1/P2 测试范围。
