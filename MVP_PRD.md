# Across Agents Assistant - MVP 产品需求文档 (PRD)

## 1. 文档目标
本文档用于定义 Across Agents Assistant 的 MVP 产品边界、目标用户、核心流程、风险控制和验收标准，确保项目以“可落地、可演示、可迭代”为优先原则推进，而不是在第一阶段追求通用全自动 Agent。

## 2. 产品概述
Across Agents Assistant 是一款面向 macOS 桌面场景的智能助手。它通过全局快捷键或状态栏入口被唤起，在用户发出语音或文本指令后，收集最小必要的系统上下文，调用大模型进行理解与规划，并在必要时通过审批界面请求用户确认后执行本地工具动作。

MVP 阶段的产品定位为：
- **受控的半自动 Agent**，不是全自动电脑接管器。
- **最小必要上下文**，不是默认抓取全屏和所有系统信息。
- **白名单工具执行**，不是开放式任意脚本执行。
- **高价值桌面任务辅助**，不是全面替代用户操作。

## 3. 战略定位
### 3.1 核心定位
- **桌面副驾**：在用户工作流旁边提供理解、建议、草稿生成和受控执行能力。
- **安全优先**：高风险动作必须显式审批，默认采取保守策略。
- **上下文增强**：充分利用前台应用、剪贴板、选中文本等信息提升回答质量。
- **系统级集成**：充分利用 macOS 原生能力，强调低延迟和轻量体验。

### 3.2 MVP 的关键取舍
- **不做全局唤醒词**：MVP 不启用 always-on 语音监听，避免误触发、权限复杂度和资源占用。
- **不做通用 UI 自动化承诺**：不承诺稳定支持所有 macOS 应用的自动操作。
- **不做开放式任意命令执行**：所有执行动作必须经过工具白名单或审批网关。
- **不做默认全量截图上传**：截图和 OCR 为按需能力，且默认关闭或需用户确认。

## 4. 目标用户与典型场景
### 4.1 目标用户
- 高频使用 macOS 桌面工作流的知识工作者。
- 以开发者、产品经理、运营、研究人员为首批适配用户。
- 愿意接受“建议优先、自动执行为辅”的交互方式。

### 4.2 核心使用场景
- 基于当前窗口和剪贴板内容回答问题。
- 对当前页面、文档、代码片段进行总结、翻译、解释、改写。
- 读取有限上下文后生成邮件草稿、消息草稿、会议纪要、待办列表。
- 对本地项目执行低风险只读操作，例如查看目录、读取文件、获取 git 状态。
- 在审批通过后执行受控动作，例如创建草稿、打开链接、写入指定工作目录文件。

### 4.3 暂不支持的场景
- 长时间后台监听的语音唤醒。
- 任意桌面应用的稳定自动点击、输入和流程编排。
- 未审批情况下的删除文件、外发消息、修改系统配置。
- 没有边界控制的 Shell/Python/Node 任意脚本执行。

## 5. 产品目标与非目标
### 5.1 MVP 目标
- 打通“触发 -> 理解 -> 补充上下文 -> 规划 -> 审批 -> 执行 -> 反馈”的完整链路。
- 在 3 到 5 个高频场景中明显提升操作效率。
- 建立稳定、可信的审批体验和任务审计能力。
- 将高风险能力严格限制在可解释、可追踪、可拒绝的边界内。

### 5.2 非目标
- 不追求跨平台。
- 不追求替代通用 IDE Agent 或系统自动化平台。
- 不追求第一版就支持所有第三方应用。
- 不追求第一版即具备复杂多步骤自治能力。

## 6. 功能范围
### 6.1 输入与唤醒
- 支持全局快捷键唤醒。
- 支持点击菜单栏图标打开主窗口。
- 支持文本输入。
- 支持按住说话式语音输入或点击开始/结束录音。
- 支持显示 STT 中间状态和识别结果。

### 6.2 渐进式上下文获取
上下文采集采用分层策略，避免一次性收集过多信息：

- **一级上下文，默认低成本采集**
  - 前台应用名称
  - 当前窗口标题
  - 当前时间、语言环境
  - 剪贴板文本

- **二级上下文，按应用适配**
  - IDE 当前文件路径
  - 光标位置
  - 选中文本
  - 浏览器当前 URL
  - Finder 当前目录

- **三级上下文，按需重型采集**
  - 屏幕截图
  - OCR 文本提取
  - 当前窗口视觉结构摘要

### 6.3 Agent 能力
- 将用户输入与上下文打包发送至 LLM。
- 支持让 LLM 输出自然语言回复、执行计划和工具调用意图。
- 支持在高风险场景中中断执行，等待用户审批。
- 支持任务结果展示、错误说明和下一步建议。

### 6.4 可执行工具范围
MVP 仅支持受控工具，不支持开放式通用执行器。实际实现的工具：

**内置工具（builtin_tools.py）：**
- `list_directory` - 列出目录内容（risk_level: low）
- `get_finder_context` - 获取 Finder 当前目录和选中文件（risk_level: low）
- `get_xcode_context` - 获取 Xcode 当前打开的文件路径（risk_level: low）
- `get_active_browser_url` - 获取 Chrome/Safari 当前标签页 URL（risk_level: low）
- `toggle_system_dark_mode` - 切换 macOS 深色/浅色模式（risk_level: low）
- `set_system_volume` - 设置系统音量 0-100（risk_level: low）
- `create_email_draft` - 在 Mail.app 创建邮件草稿（risk_level: medium）
- `create_note_draft` - 在 Notes.app 创建笔记草稿（risk_level: medium）

**MCP 工具：** 通过 MCP 服务器动态接入，例如 `local_kb__search_local_wiki`、`external_rag__query`。

**注意：**
- 文档中部分工具（如 `get_frontmost_app`、`read_clipboard`、`open_url` 等）未作为独立工具实现
- 上下文获取（如前台应用、窗口标题、剪贴板）通过 `ContextPack` 在请求时自动收集
- 风险等级使用 `"low"` / `"medium"` 字符串，而非 L0/L1/L2/L3

### 6.5 审批与安全
- 高风险动作必须弹出审批 UI。
- 审批 UI 必须展示用户原始意图、执行计划、所需工具、参数和风险级别。
- 用户可执行的审批动作包括：`approve`（同意一次）、`reject`（拒绝）、`always_allow`（始终允许该工具）。
- 所有审批结果和实际执行动作都必须记录审计日志。

**注意**：`approve_once`、`convert_to_draft`、`cancel` 选项当前未实现。

### 6.6 设置与历史
- 配置模型 API Key 或模型服务地址。
- 配置快捷键、语音输入方式和上下文采集开关。
- 配置截图/OCR 是否允许。
- 查看最近任务、审批记录和错误日志。

## 7. 关键交互流程
### 7.1 标准任务流程
1. 用户通过快捷键或状态栏触发助手。
2. 用户输入文本或语音。
3. 系统采集一级上下文。
4. Agent 判断是否需要二级或三级上下文。
5. LLM 生成回答或执行计划。
6. 若动作为高风险，则显示审批弹窗。
7. 审批通过后执行工具动作。
8. 将结果反馈给用户并记录任务日志。

### 7.2 用户拒绝审批流程
1. Agent 进入等待审批状态。
2. 用户点击拒绝或取消。
3. 任务停止执行。
4. 系统返回解释和替代建议，例如“我可以先为你生成草稿”。

### 7.3 权限未开启流程
1. 工具或上下文采集模块发现权限缺失。
2. UI 明确提示当前能力受限原因。
3. 系统提供授权引导。
4. 若用户暂不授权，则功能自动降级，不阻塞其他能力。

## 8. 风险分级与审批策略
### 8.1 风险分级
实际实现使用 `"low"` / `"medium"` 字符串，而非 L0/L1/L2/L3。

- **low：低风险只读**
  - 例如获取 Finder 上下文、Xcode 上下文、浏览器 URL、切换系统主题、设置音量。
  - 默认不审批。

- **medium：受控写入**
  - 例如创建邮件草稿、创建笔记草稿。
  - 默认需要审批。

**注意**：代码中没有 L0/L1/L2/L3 的分类。L0 纯问答不涉及工具调用，L3 高风险（如删除文件、执行终端命令）当前未实现。

### 8.2 审批原则
- 不以“工具名”单独决定是否审批，而以“动作 + 目标 + 影响范围”综合决定。
- 默认支持“生成草稿”而非“直接发送”。
- 对所有外部写入和外发行为采取保守策略。

## 9. 数据与隐私要求
- 默认采用最小必要数据采集原则。
- 屏幕截图和 OCR 必须可关闭，且应在发送前让用户明确知晓。
- 剪贴板、截图、音频等敏感数据的本地缓存应尽可能短时保留。
- 日志中不得明文长期保存高敏感原始内容，必要时应脱敏或仅存元数据。
- 用户应能够清空历史任务和本地缓存。

## 10. 已知技术边界
- 无法保证所有 macOS 应用都能稳定暴露选中文本和结构化上下文。
- Accessibility 自动化具备天然脆弱性，受 UI 变化、焦点变化和权限状态影响较大。
- 语音、截图、OCR 和 LLM 串行时容易带来明显时延，因此需要分层和异步处理。
- 如果采用开放式任意脚本执行，将很难建立可信的审批边界，因此 MVP 不采用该路线。

## 11. 成功指标
### 11.1 体验指标
- 从用户结束输入到看到首个可感知反馈的时间小于 500ms。
- 常规纯问答任务平均完成时间小于 3 秒。
- 高风险任务审批弹窗信息完整、用户可明确理解将发生什么。

### 11.2 业务指标
- 至少 3 个高频场景具备稳定复用价值。
- 首批内测用户能在一周内形成重复使用习惯。
- 任务完成率和审批通过率达到可接受水平。

### 11.3 安全指标
- 高风险操作误执行率为 0。
- 未授权场景下不发生越权读取或写入。
- 任务日志和审批日志可追踪。

## 12. MVP 里程碑
### Phase 1：基础链路
- 状态栏应用
- 全局快捷键
- 文本和语音输入
- 一级上下文采集
- LLM 对话回复

### Phase 2：受控执行
- 工具白名单
- 执行计划展示
- 审批弹窗
- 草稿型写操作
- 日志记录

### Phase 3：应用适配
- Chrome 或 Safari 适配
- Finder 适配
- IDE 基础适配
- 可选截图和 OCR

### Phase 4：体验优化
- TTS
- 权限引导优化
- 风险策略细化
- 用户配置与偏好记忆

## 13. MVP 是否可落地
结论：**可以落地，但必须坚持“受控半自动 Agent”的范围收缩策略。**

明确不可直接承诺的点：
- 不能承诺稳定支持所有应用的通用上下文抓取。
- 不能承诺通用 UI 自动化的高可靠执行。
- 不能承诺在开放式脚本模式下实现完全可信的审批拦截。

对应替代方案：
- 用“应用适配器 + 分层上下文”代替“全应用通吃”。
- 用“草稿优先 + 用户确认”代替“直接外发或直接修改”。
- 用“工具白名单 + 风险分级”代替“任意脚本执行”。
