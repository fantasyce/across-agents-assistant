# Across Agents Assistant - MVP 产品需求文档 (PRD)

## 1. 产品概述
Across Agents Assistant 是一个旨在提升开发者效率的桌面端智能助手。在 MVP 阶段，产品战略将聚焦于“受控的半自动 Agent (Controlled Semi-Auto Agent)”，以确保用户体验的可靠性和操作的可预见性，避免不可控的全自动操作带来的安全隐患和认知负担。

## 2. 核心战略与定位
- **受控的半自动 Agent (Controlled Semi-Auto Agent)**：不追求完全自主运行的 Agent，而是将其作为开发者的“副驾”。所有关键操作（如修改代码、执行破坏性命令）均需用户的显式确认。
- **取消全局唤醒词 (Removing Global Wake Word)**：为了降低误触发率和系统资源消耗，MVP 阶段不使用语音全局唤醒词（如“Hey xxx”），而是采用快捷键或点击菜单栏图标的方式唤醒助手。
- **渐进式上下文获取 (Staged Context Gathering)**：在处理用户请求时，按需、分阶段地获取上下文信息（如当前选中的代码、当前活动窗口、相关文件内容），避免一次性收集过多无效信息导致大模型 Token 浪费和处理延迟。
- **严格的工具白名单 (Strict Tool Whitelists)**：Agent 能够调用的本地工具和执行的系统命令将被严格限制在白名单内，确保系统安全。
- **采用 SwiftUI (SwiftUI over Tauri)**：客户端 UI 技术栈由 Tauri 转向原生的 SwiftUI，以获得更好的 macOS 系统集成度、更低的内存占用以及更流畅的原生动画体验。

## 3. 功能需求
### 3.1 唤醒与交互
- 支持可配置的全局快捷键唤醒。
- 支持常驻 macOS 状态栏（Menu Bar），点击图标展开交互界面。
- 文本与语音输入支持，但无全局语音持续监听与唤醒词。

### 3.2 渐进式上下文
- **第一阶段**：获取当前活动应用名称和基础系统状态。
- **第二阶段**：如果用户在 IDE 中，获取当前文件路径和光标位置/选中代码。
- **第三阶段**：根据对话需要，由 Agent 主动请求读取特定文件或执行特定命令（需在白名单内）。

### 3.3 严格的安全限制
- 内置工具白名单：仅允许读取特定类型文件、执行只读命令或特定的安全开发命令（如 `git status`, `npm run build`）。
- 危险操作（如文件修改、删除、高风险命令执行）必须通过 UI 提示用户，并等待用户点击“授权”后方可执行。

### 3.4 交互界面 (基于 SwiftUI)
- **主交互窗口**：轻量级浮窗，支持 Markdown 渲染和代码高亮。
- **授权提示框**：当 Agent 尝试执行敏感操作时弹出，清晰展示即将执行的具体动作。
- **设置面板**：配置快捷键、大模型 API Key 和工具白名单。

## 4. 成功指标
- 误触发率降至 0。
- 工具调用的安全事故发生率为 0。
- macOS 系统下内存占用降低，启动速度显著提升。